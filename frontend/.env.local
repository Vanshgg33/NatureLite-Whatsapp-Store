# Deployed backend (local frontend → deployed API)
NEXT_PUBLIC_API_URL=https://store-phi-lemon.vercel.app/api/v1

# Local backend (use when running backend locally)
# NEXT_PUBLIC_API_URL=http://localhost:7001/api/v1