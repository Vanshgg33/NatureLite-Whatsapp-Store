import { AnalyticsSnapshotRepository } from './repositories/analytics-snapshot.repository';
import { OrderRepository } from '../orders/repositories/order.repository';
import { UserRepository } from '../users/repositories/user.repository';
import { ProductRepository } from '../products/repositories/product.repository';
import { ChatSessionRepository } from '../chatbot/repositories/chat-session.repository';
import { MessageLogRepository } from '../whatsapp/repositories/message-log.repository';
import { StoreSaleRepository } from '../store-sales/repositories/store-sale.repository';
import { StoreStockRepository } from '../store-stock/repositories/store-stock.repository';
import { StoreRepository } from '../stores/repositories/store.repository';
import { AnalyticsSnapshot, SnapshotPeriod } from './schemas/analytics-snapshot.schema';
export declare class AnalyticsService {
    private readonly orderRepository;
    private readonly userRepository;
    private readonly productRepository;
    private readonly chatSessionRepository;
    private readonly messageLogRepository;
    private readonly snapshotRepository;
    private readonly storeSaleRepository;
    private readonly storeStockRepository;
    private readonly storeRepository;
    private readonly logger;
    constructor(orderRepository: OrderRepository, userRepository: UserRepository, productRepository: ProductRepository, chatSessionRepository: ChatSessionRepository, messageLogRepository: MessageLogRepository, snapshotRepository: AnalyticsSnapshotRepository, storeSaleRepository: StoreSaleRepository, storeStockRepository: StoreStockRepository, storeRepository: StoreRepository);
    generateDailySnapshot(): Promise<void>;
    generateWeeklySnapshot(): Promise<void>;
    generateMonthlySnapshot(): Promise<void>;
    private createSnapshot;
    getDashboardStats(): Promise<Record<string, unknown>>;
    getOrderMetrics(startDate: Date, endDate: Date): Promise<Record<string, unknown>>;
    getCustomerMetrics(startDate: Date, endDate: Date): Promise<Record<string, unknown>>;
    getProductMetrics(startDate: Date, endDate: Date): Promise<Record<string, unknown>>;
    getChatMetrics(startDate: Date, endDate: Date): Promise<Record<string, unknown>>;
    getRevenueByDay(days?: number): Promise<Array<{
        date: string;
        revenue: number;
        orders: number;
    }>>;
    getSnapshots(period: SnapshotPeriod, limit?: number): Promise<AnalyticsSnapshot[]>;
    getStoreDashboardStats(storeId: string): Promise<Record<string, unknown>>;
    getTodayRevenuePerStore(): Promise<any[]>;
    getMultiStoreRevenue(days?: number): Promise<any[]>;
    getStockSummaryPerStore(): Promise<any[]>;
    getTopSellingByStore(storeId: string, startDate: Date, endDate: Date, limit?: number): Promise<any[]>;
    getTopSellingOverall(startDate: Date, endDate: Date, limit?: number): Promise<any[]>;
    getMonthOverMonthByStore(): Promise<any[]>;
    getTopCustomersByStore(storeId: string, limit?: number): Promise<any[]>;
    getTopCustomersOverall(limit?: number): Promise<any[]>;
}
