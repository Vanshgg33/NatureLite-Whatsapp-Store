import { Model } from 'mongoose';
import { ChatSession, ChatSessionDocument } from './schemas/chat-session.schema';
import { WhatsAppService } from '../whatsapp/whatsapp.service';
import { UsersService } from '../users/users.service';
import { ProductsService } from '../products/products.service';
import { CategoriesService } from '../categories/categories.service';
import { CartService } from '../cart/cart.service';
import { OrdersService } from '../orders/orders.service';
import { WhatsAppMessage } from '../whatsapp/dto/whatsapp.dto';
export declare class ChatbotService {
    private chatSessionModel;
    private whatsappService;
    private usersService;
    private productsService;
    private categoriesService;
    private cartService;
    private ordersService;
    private readonly logger;
    constructor(chatSessionModel: Model<ChatSessionDocument>, whatsappService: WhatsAppService, usersService: UsersService, productsService: ProductsService, categoriesService: CategoriesService, cartService: CartService, ordersService: OrdersService);
    handleMessage(message: WhatsAppMessage): Promise<void>;
    private processInput;
    private handleMainMenu;
    private handleBrowsing;
    private handleProductDetail;
    private handleCart;
    private handleCheckout;
    private handleAddressInput;
    private handlePaymentSelection;
    private handleOrderTracking;
    private handleReorder;
    private handleFaq;
    private handleSupport;
    private sendCategoryList;
    private sendProductList;
    private sendProductDetail;
    private sendCartSummary;
    private sendCheckoutOptions;
    private sendOrdersList;
    private sendFlowResponse;
    private getOrCreateSession;
    private transitionToState;
    private updateSessionActivity;
    private extractInputText;
    private normalizeInput;
    private isMenuCommand;
    getSession(phone: string): Promise<ChatSession | null>;
    resetSession(phone: string): Promise<void>;
    cleanupExpiredSessions(): Promise<void>;
}
